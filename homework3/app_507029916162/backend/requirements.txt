# Python后端依赖包（兼容Python 3.10+）
fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.5.3

# 可选依赖
# python-dotenv==1.0.0  # 环境变量加载（可选）
# openai==1.3.0         # LLM接入（可选）
# httpx==0.26.0         # HTTP客户端（可选）
cozepy                  # Coze workflow integration

# 数据库（可选，先用内存存储）
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9

